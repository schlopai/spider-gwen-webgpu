{
  "name": "spider3-tish",
  "version": "0.1.0",
  "private": true,
  "description": "spider3 WebGPU engine ported to tish — written in tish, compiled to wasm, rendered with WebGPU.",
  "scripts": {
    "build:runtime": "bash build-runtime.sh",
    "build": "bash build.sh",
    "build:all": "bash build-runtime.sh && bash build.sh",
    "start": "node serve.mjs",
    "serve": "node serve.mjs",
    "dev": "bash build.sh && node serve.mjs",
    "setup": "bash build-runtime.sh && bash build.sh && node serve.mjs",
    "clean": "rm -rf dist/_gen dist/chunk.b64"
  },
  "engines": {
    "node": ">=18"
  }
}
